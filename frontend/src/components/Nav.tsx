"use client";

export default function Nav() {
  // TODO: Navigation bar with logo, dashboard link, profile
  return (
    <nav className="border-b px-6 py-3 flex items-center justify-between">
      <span className="font-bold text-xl">LearnOS</span>
      {/* TODO: Nav links, user avatar */}
    </nav>
  );
}
